This is a portfolio site I created back when I was learning vanilla JavaScript - no framework or libraries like React attached. A sass compiler is used to generate the style.css files.

Data on projects is entered in projects.js. Other js files are used alongside a single index.html file.

# Run project
python -m http.server
